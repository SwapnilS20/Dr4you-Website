'use strict';

/**
 * repeated-component controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::repeated-component.repeated-component');
