CREATE TABLE promise_section_heading (
    id INT AUTO_INCREMENT PRIMARY KEY,
    main_heading VARCHAR(255) NOT NULL,      -- e.g., "Our Promise to You"
    sub_heading VARCHAR(255) NOT NULL       -- e.g., "What Makes us unique"
);
